"""Module containing help information for arguments"""


class Help:
    """Class containing help information for arguments"""

    cidr = "specify the CIDR of the network to scan"
    dhcp = "specify the IP address of the DHCP of the network to scan"
    router = "specify the IP address of the router of the network to scan"
    attacker = "specify the IP address of the attacker of the network to scan"
    target = "specify the target hostname or IP address to discover"
    network = "specify the IP address of the network to scan"
    verbose = "specify the verbose flag"
    debug = "specify the debug flag"
    silent = "specify the silent flag"
